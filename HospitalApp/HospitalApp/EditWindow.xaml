<Window x:Class="HospitalApp.EditWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        mc:Ignorable="d"
        Title="Редактирование"
        Width="400" Height="500"
        WindowStartupLocation="CenterOwner"
        Background="#F5F5F5" FontFamily="Calibri">
    <Grid Margin="10">
        <Grid.RowDefinitions>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>
        <ScrollViewer VerticalScrollBarVisibility="Auto">
            <StackPanel x:Name="FieldsPanel"/>
        </ScrollViewer>
        <TextBlock x:Name="ValidationMessage" Grid.Row="1" Foreground="Red" Visibility="Collapsed" Margin="0,5,0,5" TextWrapping="Wrap"/>
        <StackPanel Grid.Row="2" Orientation="Horizontal" HorizontalAlignment="Right">
            <Button x:Name="SaveButton" Content="Сохранить" Width="100" Margin="0,0,10,0" Click="Save_Click" Background="#0078D7" Foreground="White"/>
            <Button x:Name="CancelButton" Content="Отмена" Width="100" Click="Cancel_Click" Background="#D3D3D3"/>
        </StackPanel>
    </Grid>
</Window>