<Window x:Class="HospitalApp.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:HospitalApp"
        xmlns:sys="clr-namespace:System;assembly=mscorlib"
        mc:Ignorable="d" Icon="/Resources/logo.ico" ShowInTaskbar="True"
        Title='АИС "№20"' FontFamily="Calibri" MinHeight="350" MinWidth="600" MaxHeight="400" MaxWidth="1000">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="44"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="120" Name="MenuColumn"/>
            <ColumnDefinition Width="*"/>
        </Grid.ColumnDefinitions>

        <Grid Name="HeaderGrid" Grid.Row="0" Grid.Column="0" Grid.ColumnSpan="2" Background="#6EDC82" Visibility="{Binding HeaderVisibility}">
            <Button Name="BurgerButton" Background="Transparent" BorderThickness="0" VerticalAlignment="Center" 
                    HorizontalAlignment="Left" Margin="80,0,0,0" Height="30" Width="30" Click="BurgerButton_Click">
                <Image Source="/Resources/BurgerIcon.png" Stretch="Fill"></Image>
            </Button>
            <Image Source="/Resources/logo.png" VerticalAlignment="Center" HorizontalAlignment="Left" Grid.Column="0" Width="40" Margin="5,0,0,0" Panel.ZIndex="10"/>
            <Border CornerRadius="12" VerticalAlignment="Center" HorizontalAlignment="Right" Margin="0,0,10,0" Background="#468C50" Width="160" Height="35" Grid.Row="0" Grid.Column="1">
                <Grid Name="ProfileGrid">
                    <Button Name="ProfileBtn" VerticalAlignment="Center" HorizontalAlignment="Left" Width="30" Height="30" Margin="5,0,0,0" Background="Transparent" BorderThickness="0" Click="ProfileBtn_Click">
                        <Image Source="{Binding UserProfileIcon}" Stretch="Fill">
                            <Image.Clip>
                                <EllipseGeometry Center="15,15" RadiusX="15" RadiusY="15"/>
                            </Image.Clip>
                        </Image>
                    </Button>
                    <TextBlock Text="{Binding UserFullName}" Foreground="White" VerticalAlignment="Center" HorizontalAlignment="Left" Margin="45,0,0,0" FontSize="12"/>
                    <Button Name="ExitProfileBtn" FontSize="12" Width="25" Height="25" Content="Exit" VerticalAlignment="Center" HorizontalAlignment="Right" Background="Transparent" BorderThickness="0" Foreground="White" Margin="0,0,10,0" Click="ExitBtn_Click"/>
                </Grid>
            </Border>
        </Grid>

        <Grid Name="MenuGrid" Grid.Column="0" Background="#6EDC82" Grid.Row="1" Visibility="{Binding MenuVisibility}">
            <Button Name="MainBtn" Content="Main Menu" FontSize="18" Padding="5" Grid.Row="0" Grid.Column="0" VerticalAlignment="Top" BorderThickness="0" Height="50" Margin="0,0,0,0" Background="#6EDC82"/>
            <Button Name="ServiceBtn" Content="Service" FontSize="18" Padding="5" Background="#6EDC82" VerticalAlignment="Top" Height="50" BorderThickness="0" Margin="0,50,0,0" Grid.Row="1"/>
            <Button Name="OrderBtn" Content="Order" FontSize="18" Padding="5" Background="#6EDC82" VerticalAlignment="Top" Height="50" BorderThickness="0" Margin="0,100,0,0" Grid.Row="1"/>
            <Button Name="PacientBtn" Content="Pacient" FontSize="18" Padding="5" VerticalAlignment="Top" Height="50" Margin="0,150,0,0" BorderThickness="0" Background="#6EDC82" Grid.Row="1"/>
            <Button Name="ExitBtn" Content="Exit" FontSize="18" Padding="5" VerticalAlignment="Bottom" Height="50" Margin="0,0,0,0" BorderThickness="0" Background="#6EDC82" Grid.Row="1" Click="ExitBtn_Click"/>
        </Grid>
        <Frame Name="MainFrame" NavigationUIVisibility="Hidden" Grid.Column="1" Grid.Row="1" Grid.ColumnSpan="1" Navigated="MainFrame_Navigated"/>
    </Grid>
</Window>